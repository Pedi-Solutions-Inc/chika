{
  "name": "chika-server",
  "version": "1.0.7",
  "private": true,
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "test": "bun test",
    "test:watch": "bun test --watch"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.5.0",
    "@pedi/chika-types": "workspace:*",
    "@sentry/bun": "^10.46.0",
    "hono": "^4.12.9",
    "hono-rate-limiter": "^0.5.3",
    "mongodb": "^6.12.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "mongodb-memory-server": "^10.4.0"
  }
}
